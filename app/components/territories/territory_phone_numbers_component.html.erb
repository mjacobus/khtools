<% if show_details? %>
  <details>
    <summary>
<% end %>
<%= render AttributeComponent.new(**options) do value end %>
<% if show_details? %>
    <%= render CopyToClipboardComponent.new(selector: ".#{bem('phones')}") %>
    </summary>
    <div class="<%= bem('phones') %>"><%= record.phone_numbers.join("\n") %></div>
    
    <% record.phone_numbers.each do |phone| %>
      <div><%= phone %></div>
    <% end %>
  </details>
<% end %>
