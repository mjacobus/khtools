<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">
      <%= image_tag("#{current_user.avatar}?sz=25", class: 'avatar-25') %> <%= current_user.name %>
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link" href="<%= root_path %>">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="<%= meeting_attendance_meetings_url %>"><%= t('app.links.meeting_attendance') %></a>
        </li>
        <% if current_user.master? %>
          <li class="nav-item">
            <a class="nav-link" href="<%= admin_dashboard_url %>">Admin</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="<%= users_path %>"><%= t('app.links.users') %></a>
          </li>
        <% end %>
        <li class="nav-item">
          <a class="nav-link" href="/logout"><%= t('app.links.logout') %></a>
        </li>
      </ul>
    </div>
  </div>
</nav>
